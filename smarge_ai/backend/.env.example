# SmargeAI Backend Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# GOOGLE GEMINI API KEY (REQUIRED)
# ============================================
# Get your free API key at: https://makersuite.google.com/app/apikey
# The free tier includes generous limits for development and testing
GOOGLE_API_KEY=your_google_api_key_here

# ============================================
# JWT SECRET KEY (REQUIRED FOR PRODUCTION)
# ============================================
# Used to sign JWT tokens for user authentication
# Generate a secure random string using: openssl rand -hex 32
# NEVER commit the actual secret key to version control
SECRET_KEY=your_secret_key_change_this_in_production

# ============================================
# DATABASE URL (OPTIONAL)
# ============================================
# Default: SQLite database in the current directory
# For production, consider using PostgreSQL
#
# SQLite (default):
DATABASE_URL=sqlite+aiosqlite:///./smarge_ai.db
#
# PostgreSQL example:
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/smarge_ai
#
# PostgreSQL with SSL:
# DATABASE_URL=postgresql+asyncpg://username:password@host:5432/dbname?ssl=require
